query UserTimelogs {
  currentUser {
    timelogs {
      nodes {
        id
        spentAt
        project {
          name
        }
        issue {
          id,
          title
        }
        timeSpent
      }
    }
  }
}

